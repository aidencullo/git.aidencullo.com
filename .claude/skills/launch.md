# Launch Local Development Server

Start the local development server for the stats React/Vite application.

## Instructions

1. Navigate to the `stats` directory
2. Install dependencies with `npm install`
3. Run `npm run dev` to start the Vite dev server (opens browser automatically)

## Command

```bash
cd stats && npm install && npm run dev
```
